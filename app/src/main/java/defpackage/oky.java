package defpackage;

abstract class oky implements old {
    oky() {
    }

    @Override
    public final olb a(CharSequence charSequence) {
        int length = charSequence.length();
        int i = length + length;
        ohr.a(i >= 0, "expectedInputSize must be >= 0 but was %s", i);
        return a().a(charSequence).a();
    }
}