package defpackage;

import android.hardware.camera2.CaptureRequest;

public interface mpy extends mpp {
    Object a();

    Object a(CaptureRequest.Key key);

    boolean equals(Object obj);

    int hashCode();
}