package defpackage;

import android.hardware.camera2.CaptureRequest;
import java.util.Set;

public interface mcs {
    mcr a();

    mcs a(CaptureRequest.Key key, Object obj);

    mcs a(Set set);

    mcs a(mcz mczVar);
}